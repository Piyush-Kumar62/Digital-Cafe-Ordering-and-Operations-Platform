# spring-dotenv runtime config
# Load .env from the current working directory (backend module root).
# This keeps IntelliJ, VS Code, and STS consistent when run from digital-cafe-backend.
directory=.
filename=.env
ignoreIfMalformed=false
ignoreIfMissing=true
systemProperties=false

